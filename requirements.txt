# 核心运行时依赖 —— 与 pyproject.toml [project.dependencies] 完全一致。
# 版本下限参考当前实际安装版本; 主代码路径 import 已逐一 grep 确认。
akshare>=1.18.0
pandas>=3.0.0
numpy>=2.0.0
scipy>=1.18.0
lightgbm>=4.7.0
pyyaml>=6.0
requests>=2.34.0
pyarrow>=25.0.0
matplotlib>=3.11.0
fastapi>=0.141.0
uvicorn>=0.51.0
httpx>=0.28.0

# 可选 —— 东财接口代理网关 (eastmoney_proxy.py); 未安装则静默直连, 对应 pyproject [proxy]
# akshare-proxy-patch>=0.5.0

# 说明 —— 以下为原清单误列、但主代码路径未使用的依赖, 本次清理移除:
#   openai    : 全仓库零引用 (连 archive 都无)。
#   apscheduler: 仅 archive/legacy/scheduler.py; 现由系统调度器跑 daily_pipeline。
#   streamlit : 仅 archive/dashboard.py; 现前端为 web/ui (React + Vite)。
#   loguru/tqdm: 仅 archive/src_quant/utils; 已从 pyproject 一并移除。
# torch>=2.0.0  # 深度学习预留, 暂不安装
